#include <bits/stdc++.h>
#include <ios>

void solve() {}

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(NULL);

  int32_t t;
  std::cin >> t;

  while (t--) {
    std::cin >> n;
    std::cin >> a;

    solve();
  }

  return 0;
}
